<?xml version="1.0" encoding="UTF-8"?>
<!--
  Phase 4 observability: console JSON logs via LogstashEncoder. Micrometer's
  OTel bridge populates MDC keys 'traceId' and 'spanId' on every request thread;
  we surface them as top-level fields so a downstream collector can correlate.
  The 'test' profile reverts to Spring Boot's default plain-text pattern so unit
  test output stays human-readable.
-->
<configuration>

    <springProfile name="!test">
        <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
                <providers>
                    <timestamp>
                        <fieldName>@timestamp</fieldName>
                        <timeZone>UTC</timeZone>
                    </timestamp>
                    <version/>
                    <logLevel/>
                    <loggerName/>
                    <threadName/>
                    <message/>
                    <mdc>
                        <includeMdcKeyName>traceId</includeMdcKeyName>
                        <includeMdcKeyName>spanId</includeMdcKeyName>
                        <includeMdcKeyName>userId</includeMdcKeyName>
                        <includeMdcKeyName>requestId</includeMdcKeyName>
                    </mdc>
                    <stackTrace/>
                    <pattern>
                        <pattern>{"application":"${spring.application.name:-issueflow}"}</pattern>
                    </pattern>
                </providers>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="JSON"/>
        </root>
    </springProfile>

    <springProfile name="test">
        <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
        <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

</configuration>
